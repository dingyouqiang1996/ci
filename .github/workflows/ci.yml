name: Build and Test Docker Image

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Docker Login
        run: |
          echo '${{ secrets.DOCKER_PULL_REGISTRY_SECRET }}' | docker login hkccr.ccs.tencentyun.com --username=${{ secrets.TX_TCR_UID }} --password-stdin
      - name: Build Docker image
        run: |
          docker build -t hkccr.ccs.tencentyun.com/rasp/ci:bilibili-v1 ./bilibili
      - name: Push Docker image
        run: |
          docker push hkccr.ccs.tencentyun.com/rasp/ci:bilibili-arm64-v1
